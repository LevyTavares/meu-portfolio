{
  "name": "meu-portfolio-api",
  "version": "1.0.0",
  "description": "Cloudflare Workers API para portfólio",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "resend": "^3.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240512.0",
    "typescript": "^5.4.0",
    "wrangler": "^3.48.0"
  }
}
